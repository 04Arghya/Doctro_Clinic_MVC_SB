<%-- src/main/webapp/WEB-INF/jsp/adminDashboard.jsp --%>
<%@ page contentType="text/html;charset=UTF-8" language="java" %>
<%@ taglib prefix="c" uri="http://java.sun.com/jsp/jstl/core" %>

<html>
<head>
    <title>Admin Dashboard</title>
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css">
    <link rel="stylesheet" href="/style.css"> <%-- Link to your custom CSS --%>
    <style>
        /* Optional: Basic styling for the doctor photo in the table */
        .doctor-table-photo {
            width: 50px; /* Adjust as needed */
            height: 50px; /* Adjust as needed */
            object-fit: cover; /* Ensures image covers the area without distortion */
            border-radius: 5px; /* Slightly rounded corners */
            vertical-align: middle; /* Align with text */
        }
    </style>
</head>
<body>
    <jsp:include page="common/header.jsp" /> <%-- Include Header --%>

    <div class="container">
        <div class="dashboard-header">
            <h2 class="text-primary">Admin Dashboard</h2>
            <%-- Button to navigate to the Add New Doctor form --%>
            <a href="/admin/addDoctor" class="btn btn-success">Add New Doctor</a>
        </div>

        <p class="mb-4">Welcome, ${loggedInUser.name}!</p>

        <%-- Display success/error messages after redirect --%>
        <c:if test="${not empty successMessage}">
            <div class="alert alert-success" role="alert">${successMessage}</div>
        </c:if>
        <c:if test="${not empty error}">
            <div class="alert alert-danger" role="alert">${error}</div>
        </c:if>

        <h3 class="mt-4 mb-3">All Appointments Summary</h3>

        <c:if test="${empty patientDoctorMap}">
            <div class="alert alert-info" role="alert">
                No appointments found.
            </div>
        </c:if>
        <c:if test="${not empty patientDoctorMap}">
            <table class="table table-bordered table-striped">
                <thead class="table-dark">
                    <tr>
                        <th>Patient Name</th>
                        <th>Doctor(s) Assigned</th>
                    </tr>
                </thead>
                <tbody>
                    <c:forEach var="entry" items="${patientDoctorMap}">
                        <tr>
                            <td>${entry.key.name}</td>
                            <td>
                                <c:forEach var="docName" items="${entry.value}">
                                    ${docName}<br />
                                </c:forEach>
                            </td>
                        </tr>
                    </c:forEach>
                </tbody>
            </table>
        </c:if>

        <h3 class="mt-4 mb-3">Registered Doctors</h3>
        <c:if test="${empty doctors}">
            <div class="alert alert-info" role="alert">
                No doctors registered yet.
            </div>
        </c:if>
        <c:if test="${not empty doctors}">
            <table class="table table-bordered table-striped">
                <thead class="table-info">
                    <tr>
                        <th>ID</th>
                        <th>Name</th>
                        <th>Specialization</th>
                        <th>Experience</th>
                        <th>Photo</th> <%-- Changed from Photo URL --%>
                        <th>Linked User Email</th>
                    </tr>
                </thead>
                <tbody>
                    <c:forEach var="doctor" items="${doctors}">
                        <tr>
                            <td>${doctor.id}</td>
                            <td>${doctor.name}</td>
                            <td>${doctor.specialization}</td>
                            <td>${doctor.experience} years</td>
                            <%-- --- START FIX: Display photo instead of URL --- --%>
                            <td>
                                <img src="${doctor.photoUrl}" alt="${doctor.name} Photo" class="doctor-table-photo">
                            </td>
                            <%-- --- END FIX --- --%>
                            <td>${doctor.user.email}</td>
                        </tr>
                    </c:forEach>
                </tbody>
            </table>
        </c:if>
    </div>

    <footer class="footer">
        <div class="container">
            <p>&copy; 2025 Orgos Doctor Clinic. All rights reserved.</p>
        </div>
    </footer>

    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/js/bootstrap.bundle.min.js"></script>
</body>
</html>